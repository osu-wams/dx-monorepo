import {
  shadows,
  gradients,
  breakpoints,
  spacing,
  fontSize,
  borderRadius,
  mq,
} from './theme-settings';
import { Color } from './theme-colors';
import type { ThemeConfiguration } from './theme-interface'; // eslint-disable-line
import GlobalStyles from './GlobalStyles';

export {
  shadows,
  spacing,
  gradients,
  breakpoints,
  mq,
  GlobalStyles,
  fontSize,
  borderRadius,
  Color,
};

export type { ThemeConfiguration };
